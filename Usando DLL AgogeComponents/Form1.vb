Public Class Form1
    Inherits AgogeComponents.FormularioMDI

    Sub New()
        MyBase.New(Form2)
        ' This call is required by the designer.
        InitializeComponent()

        ' Add any initialization after the InitializeComponent() call.

    End Sub
End Class
